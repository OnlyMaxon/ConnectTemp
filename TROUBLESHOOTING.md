# 🔧 Руководство по устранению неполадок

## 📱 Проблемы с NFC

### ❌ "NFC not supported"

**Проблема:** Устройство не поддерживает NFC

**Решения:**
1. Проверьте спецификации вашего устройства
2. Убедитесь, что это не эмулятор
3. Некоторые старые устройства не имеют NFC модуля

**Примечание:** Эмуляторы НИКОГДА не поддерживают NFC

---

### ❌ "NFC is disabled"

**Проблема:** NFC выключен в настройках

**Решение для Android:**
```
1. Откройте Настройки
2. Подключения (или "Еще" / "Дополнительно")
3. NFC и бесконтактные платежи
4. Включите переключатель NFC
```

**Решение для iOS:**
NFC всегда включен на поддерживаемых устройствах (iPhone 7+)

---

### ❌ NFC не читает данные

**Возможные причины:**

1. **Неправильное положение телефонов**
   - Android: антенна в центре задней панели
   - iOS: антенна в верхней части (возле камеры)
   - Расстояние должно быть 2-4 см

2. **Чехол блокирует сигнал**
   - Снимите металлические или толстые чехлы
   - Некоторые чехлы имеют металлические вставки

3. **Приложение не активно**
   - iOS требует активное приложение на переднем плане
   - Убедитесь, что экран включен

4. **Низкий заряд батареи**
   - Некоторые устройства отключают NFC при <15% заряда

**Решение:**
```bash
# Проверка в коде
const enabled = await isNFCEnabled();
console.log('NFC enabled:', enabled);

const supported = await initNFC();
console.log('NFC supported:', supported);
```

---

### ❌ "NFC session timed out" (iOS)

**Проблема:** Сессия NFC на iOS ограничена 60 секундами

**Решение:**
1. Не держите сессию открытой долго
2. Закройте и откройте сессию заново
3. Попросите пользователя нажать кнопку снова

```typescript
// В коде это обрабатывается автоматически
await stopNFCListener();
await startNFCListener(callback);
```

---

### ❌ Передача не происходит между устройствами

**Чеклист:**

- [ ] Оба устройства имеют установленное приложение
- [ ] Оба пользователя создали профиль
- [ ] Оба нажали кнопку "Обменяться контактами"
- [ ] NFC включен на обоих устройствах
- [ ] Телефоны приложены правильно
- [ ] Нет толстых чехлов
- [ ] Экраны включены

---

## 🔨 Проблемы установки

### ❌ "Cannot find module 'react'"

**Проблема:** Зависимости не установлены

**Решение:**
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

---

### ❌ "expo command not found"

**Проблема:** Expo CLI не установлен глобально

**Решение:**
```bash
npm install -g expo-cli

# Или используйте npx
npx expo start
```

---

### ❌ Ошибки при установке на Windows

**Проблема:** Длинные пути в node_modules

**Решение:**
```bash
# Включите длинные пути
npm config set script-shell "powershell.exe"

# Или установите в папку с коротким путем
cd C:\projects
git clone ...
```

---

### ❌ "Metro bundler failed to start"

**Проблема:** Порт занят или кэш поврежден

**Решение:**
```bash
# Очистите кэш
npx expo start --clear

# Или используйте другой порт
npx expo start --port 8082
```

---

## 📱 Проблемы с Android

### ❌ "Unable to load script"

**Проблема:** Metro bundler не доступен для устройства

**Решение:**
```bash
# Убедитесь что устройство в той же WiFi сети
# Или используйте туннель
npx expo start --tunnel

# Проверьте IP адрес компьютера
ipconfig  # Windows
ifconfig  # Mac/Linux
```

---

### ❌ Приложение крашится при запуске NFC

**Проблема:** Разрешения NFC не настроены

**Решение:**
1. Проверьте `AndroidManifest.xml`:
```xml
<uses-permission android:name="android.permission.NFC" />
```

2. Пересоберите приложение:
```bash
cd android
./gradlew clean
cd ..
npx expo start
```

---

### ❌ "App not installed"

**Проблема:** Конфликт с предыдущей версией

**Решение:**
```bash
# Удалите приложение с устройства
adb uninstall com.nfccontactshare.app

# Переустановите
npm run android
```

---

### ❌ Gradle build failed

**Проблема:** Проблемы с Gradle кэшем или версией

**Решение:**
```bash
cd android

# Очистите Gradle кэш
./gradlew clean
./gradlew --stop

# Удалите кэш
rm -rf .gradle
rm -rf build

cd ..

# Пересоберите
npx expo prebuild --clean
npm run android
```

---

## 🍎 Проблемы с iOS

### ❌ "CocoaPods not installed"

**Проблема:** CocoaPods не установлен (только Mac)

**Решение:**
```bash
# Установите CocoaPods
sudo gem install cocoapods

# Или через Homebrew
brew install cocoapods
```

---

### ❌ "Pod install failed"

**Проблема:** Проблемы с зависимостями pods

**Решение:**
```bash
cd ios

# Удалите кэш
rm -rf Pods
rm -rf ~/Library/Caches/CocoaPods
rm -rf ~/Library/Developer/Xcode/DerivedData

# Переустановите
pod deintegrate
pod install

cd ..
```

---

### ❌ "Signing requires a development team"

**Проблема:** Не настроен Apple Developer аккаунт

**Решение:**
1. Откройте `ios/NFCContactShare.xcworkspace` в Xcode
2. Выберите проект → Target → Signing & Capabilities
3. Выберите ваш Team (нужен Apple ID)
4. Для тестирования достаточно бесплатного аккаунта

---

### ❌ "Near Field Communication Tag Reading" не работает

**Проблема:** Capability не добавлена

**Решение:**
1. В Xcode: Target → Signing & Capabilities
2. Нажмите "+ Capability"
3. Найдите и добавьте "Near Field Communication Tag Reading"
4. Пересоберите проект

---

### ❌ Приложение не реагирует на NFC на iPhone

**Возможные причины:**

1. **iPhone слишком старый**
   - Требуется iPhone 7 или новее
   - iPhone 6/6 Plus не поддерживают запись NFC

2. **Неправильное положение**
   - Антенна в ВЕРХНЕЙ части iPhone
   - Приложите верхние части телефонов

3. **Приложение в фоне**
   - iOS требует активное приложение
   - Откройте приложение и держите на переднем плане

4. **Не добавлено в Info.plist**
```xml
<key>NFCReaderUsageDescription</key>
<string>This app uses NFC to share contacts</string>
```

---

## 💾 Проблемы с хранением данных

### ❌ Данные не сохраняются

**Проблема:** AsyncStorage не работает

**Отладка:**
```typescript
// Добавьте логи в storage.ts
export const saveUserProfile = async (profile: UserProfile) => {
  try {
    console.log('Saving profile:', profile);
    await AsyncStorage.setItem(PROFILE_KEY, JSON.stringify(profile));
    console.log('Profile saved successfully');
  } catch (error) {
    console.error('Error saving profile:', error);
    throw error;
  }
};
```

**Решение:**
1. Проверьте место на устройстве
2. Проверьте разрешения приложения
3. Переустановите приложение

---

### ❌ Контакты исчезли после перезапуска

**Проблема:** AsyncStorage очистился

**Причины:**
- Переустановка приложения
- Очистка данных приложения
- Обновление системы

**Решение:**
В будущих версиях планируется:
- Экспорт контактов
- Резервное копирование

---

## 🔍 Отладка

### Включение режима отладки

**React Native Debugger:**
```bash
# В приложении нажмите Ctrl+M (Android) или Cmd+D (iOS)
# Выберите "Debug"
```

**Chrome DevTools:**
```bash
# В приложении откройте меню разработчика
# Выберите "Debug JS Remotely"
# Откроется Chrome с консолью
```

**Логи:**
```typescript
// Добавьте логи в критических местах
console.log('NFC reading started');
console.log('Received profile:', profile);
console.log('Saving contact:', contact);
```

---

### Просмотр логов устройства

**Android:**
```bash
# Все логи
adb logcat

# Фильтр по приложению
adb logcat | grep "com.nfccontactshare"

# Очистить и начать заново
adb logcat -c
adb logcat
```

**iOS:**
```bash
# В Xcode
Window → Devices and Simulators → View Device Logs

# Или в терминале
idevicesyslog
```

---

### Тестирование NFC без второго устройства

**Используйте NFC метки:**
1. Купите программируемые NFC метки (NTAG213/215/216)
2. Используйте приложение для записи NDEF
3. Запишите тестовые данные в формате JSON
4. Проверьте чтение меткой

**Приложения для записи:**
- Android: "NFC Tools"
- iOS: "NFC Tools"

---

## 🐛 Сообщение об ошибках

Если проблема не решена:

1. **Соберите информацию:**
   - Версия приложения
   - Модель устройства
   - Версия Android/iOS
   - Шаги для воспроизведения
   - Логи ошибок

2. **Проверьте документацию:**
   - README.md
   - FAQ.md
   - Этот файл

3. **Создайте issue:**
   - Опишите проблему детально
   - Приложите логи
   - Укажите среду

---

## ✅ Проверочный список перед обращением в поддержку

- [ ] Прочитал README.md
- [ ] Проверил FAQ.md
- [ ] Проверил этот файл (TROUBLESHOOTING.md)
- [ ] Переустановил зависимости (`npm install`)
- [ ] Очистил кэш (`npx expo start --clear`)
- [ ] Перезагрузил устройство
- [ ] NFC включен в настройках
- [ ] Используется реальное устройство (не эмулятор)
- [ ] Собрал логи ошибок

---

## 🔗 Полезные ссылки

- [React Native Docs](https://reactnative.dev/docs/getting-started)
- [Expo Docs](https://docs.expo.dev/)
- [NFC Manager](https://github.com/revtel/react-native-nfc-manager)
- [React Navigation](https://reactnavigation.org/docs/getting-started)

---

**Версия:** 1.0.0  
**Последнее обновление:** 19 ноября 2025
