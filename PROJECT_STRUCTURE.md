# Структура проекта NFC Contact Share

## 📁 Полная структура файлов

```
NFCContactShare/
│
├── 📱 src/                          # Исходный код приложения
│   ├── screens/                     # Экраны приложения
│   │   ├── HomeScreen.tsx           # Главный экран с NFC и списком контактов
│   │   ├── ProfileScreen.tsx        # Создание/редактирование профиля
│   │   ├── ReceivedContactScreen.tsx # Просмотр полученного контакта
│   │   └── ContactsListScreen.tsx   # Детальный список контактов
│   │
│   └── services/                    # Сервисы и бизнес-логика
│       ├── storage.ts               # Работа с AsyncStorage (профиль, контакты)
│       └── nfc.ts                   # NFC функционал (чтение/запись)
│
├── 🎨 assets/                       # Ресурсы приложения
│   ├── icon.png                     # Иконка приложения (1024x1024)
│   ├── splash.png                   # Загрузочный экран
│   ├── adaptive-icon.png            # Адаптивная иконка (Android)
│   ├── favicon.png                  # Favicon (Web)
│   └── index.js                     # Экспорт ресурсов
│
├── 📱 App.tsx                       # Главный компонент приложения с навигацией
│
├── ⚙️ Конфигурационные файлы
│   ├── package.json                 # Зависимости и скрипты npm
│   ├── app.json                     # Конфигурация Expo
│   ├── babel.config.js              # Конфигурация Babel
│   ├── tsconfig.json                # Конфигурация TypeScript
│   └── .gitignore                   # Игнорируемые файлы Git
│
├── 📚 Документация
│   ├── README.md                    # Основная документация
│   ├── FAQ.md                       # Часто задаваемые вопросы
│   ├── ANDROID_SETUP.md             # Инструкции для Android
│   ├── IOS_SETUP.md                 # Инструкции для iOS
│   ├── BUILD_GUIDE.md               # Руководство по сборке
│   ├── CHANGELOG.md                 # История изменений
│   ├── LICENSE                      # Лицензия MIT
│   └── PROJECT_STRUCTURE.md         # Этот файл
│
└── 🔧 Генерируемые папки (после установки)
    ├── node_modules/                # Зависимости npm (не в git)
    ├── .expo/                       # Кэш Expo (не в git)
    ├── ios/                         # Нативный код iOS (после prebuild)
    └── android/                     # Нативный код Android (после prebuild)
```

---

## 📋 Описание основных компонентов

### 🎯 Экраны (src/screens/)

#### HomeScreen.tsx
**Назначение:** Главный экран приложения  
**Функционал:**
- Отображение статуса NFC
- Кнопка "Обменяться контактами"
- Список последних сохраненных контактов
- Индикатор прослушивания NFC
- Навигация к профилю и списку контактов

**Состояния:**
- `nfcSupported` - поддерживается ли NFC
- `nfcEnabled` - включен ли NFC
- `isListening` - идет ли прослушивание
- `hasProfile` - создан ли профиль
- `contacts` - список контактов

#### ProfileScreen.tsx
**Назначение:** Создание и редактирование профиля пользователя  
**Функционал:**
- Форма ввода личных данных
- Добавление обязательных полей
- Добавление опциональных полей
- Динамическое добавление дополнительных ссылок
- Валидация данных
- Сохранение в AsyncStorage

**Поля формы:**
- firstName, lastName (обязательные)
- email, phone (обязательные)
- linkedin, github (опциональные)
- customLinks[] (динамический массив)

#### ReceivedContactScreen.tsx
**Назначение:** Просмотр и сохранение полученного контакта  
**Функционал:**
- Отображение всех данных контакта
- Кнопка "Сохранить контакт"
- Кнопка "Закрыть"
- Интерактивные ссылки (можно кликнуть)
- Открытие внешних приложений (Email, Телефон)

**Props:**
- `route.params.contact` - объект UserProfile

#### ContactsListScreen.tsx
**Назначение:** Управление сохраненными контактами  
**Функционал:**
- Список всех сохраненных контактов
- Удаление контактов
- Просмотр деталей контакта
- Отображение даты сохранения

---

### 🔧 Сервисы (src/services/)

#### storage.ts
**Назначение:** Управление локальным хранилищем  
**Функции:**

```typescript
// Типы
interface UserProfile {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  linkedin?: string;
  github?: string;
  customLinks: Array<{ label: string; url: string }>;
}

interface SavedContact extends UserProfile {
  id: string;
  savedAt: number;
}

// API
saveUserProfile(profile: UserProfile): Promise<void>
getUserProfile(): Promise<UserProfile | null>
saveContact(contact: UserProfile): Promise<void>
getSavedContacts(): Promise<SavedContact[]>
deleteContact(contactId: string): Promise<void>
```

**Хранилище:**
- `@user_profile` - профиль пользователя
- `@saved_contacts` - массив контактов

#### nfc.ts
**Назначение:** Работа с NFC  
**Функции:**

```typescript
// API
initNFC(): Promise<boolean>
isNFCEnabled(): Promise<boolean>
writeNFC(profile: UserProfile): Promise<boolean>
readNFC(): Promise<UserProfile | null>
startNFCListener(onContactReceived: (profile: UserProfile) => void): Promise<void>
stopNFCListener(): Promise<void>
cleanupNFC(): Promise<void>
```

**Протокол:**
- Формат: NDEF (NFC Data Exchange Format)
- Идентификатор: `com.nfccontactshare.app`
- Тип записи: Text Record с JSON

**Процесс обмена:**
1. Пользователь A нажимает кнопку → `startNFCListener()`
2. Пользователь B прикладывает телефон
3. NFC обнаружен → `readNFC()` читает данные
4. Проверка идентификатора приложения
5. Парсинг JSON → получение UserProfile
6. Callback `onContactReceived()` → открытие ReceivedContactScreen

---

## 🎨 Дизайн-система

### Цвета

```typescript
Primary: '#007AFF'     // Основной синий (iOS стиль)
Success: '#34C759'     // Зеленый для успеха
Danger: '#FF3B30'      // Красный для ошибок
Warning: '#FFC107'     // Желтый для предупреждений
Background: '#F5F5F5'  // Светло-серый фон
Card: '#FFFFFF'        // Белый для карточек
Text: '#333333'        // Темный текст
TextLight: '#666666'   // Светлый текст
TextLighter: '#999999' // Еще светлее
```

### Типографика

```typescript
Title: 28px, Bold      // Заголовки экранов
Subtitle: 20px, Bold   // Подзаголовки
Body: 16px, Regular    // Основной текст
Caption: 14px, Regular // Подписи
Small: 12px, Regular   // Мелкий текст
```

### Отступы

```typescript
xs: 5px
sm: 10px
md: 15px
lg: 20px
xl: 30px
```

### Компоненты

- **Button:** borderRadius: 10-15px, padding: 15-20px
- **Card:** borderRadius: 10-12px, shadow, padding: 15-20px
- **Input:** borderRadius: 10px, padding: 15px, border: 1px

---

## 📦 Зависимости

### Основные

```json
{
  "expo": "~51.0.0",
  "react": "18.2.0",
  "react-native": "0.74.5"
}
```

### Навигация

```json
{
  "@react-navigation/native": "^6.1.9",
  "@react-navigation/stack": "^6.3.20",
  "react-native-screens": "~3.31.1",
  "react-native-safe-area-context": "4.10.5",
  "react-native-gesture-handler": "~2.16.1"
}
```

### Функционал

```json
{
  "@react-native-async-storage/async-storage": "1.23.1",
  "react-native-nfc-manager": "^3.14.0"
}
```

---

## 🔄 Потоки данных

### Создание профиля

```
User → ProfileScreen
  → Fill form
  → Press "Сохранить"
  → saveUserProfile()
  → AsyncStorage.setItem('@user_profile')
  → Navigate back to HomeScreen
  → HomeScreen reloads → hasProfile = true
```

### Обмен контактами

```
Device A (Sender):
User → Press "Обменяться контактами"
  → startNFCListener()
  → getUserProfile()
  → Wait for NFC tag

Device B (Receiver):
Phone with NFC tag approaches
  → NFC detected
  → readNFC()
  → Parse NDEF message
  → Check app identifier
  → Extract UserProfile
  → onContactReceived(profile)
  → Navigate to ReceivedContactScreen

User on Device B:
  → View contact details
  → Press "Сохранить"
  → saveContact()
  → Navigate to HomeScreen
```

### Просмотр контактов

```
HomeScreen loaded
  → getSavedContacts()
  → Display in FlatList
  
User clicks contact
  → Navigate to ReceivedContactScreen
  → Display full details
  
User clicks link
  → Linking.openURL()
  → Open external app
```

---

## 🛠️ Команды для разработки

```bash
# Установка
npm install

# Запуск
npm start                    # Expo Dev Server
npm run android             # Android
npm run ios                 # iOS (только Mac)

# Полезные команды
npx expo start --clear      # Очистить кэш
npx expo prebuild          # Генерация нативного кода
npx expo doctor            # Проверка проблем

# Тестирование на устройстве
# 1. Установите Expo Go на телефон
# 2. Сканируйте QR код после npm start
```

---

## 🎓 Архитектурные решения

### Почему Expo?

✅ Быстрая разработка  
✅ Простая настройка  
✅ Встроенные модули  
✅ Легкая сборка через EAS  
❌ Некоторые нативные ограничения

### Почему AsyncStorage?

✅ Простой API  
✅ Встроенная поддержка в React Native  
✅ Не требует сервера  
✅ Достаточно для небольшого объема данных  
❌ Не подходит для больших данных

### Почему TypeScript?

✅ Типобезопасность  
✅ Автодополнение в IDE  
✅ Раннее обнаружение ошибок  
✅ Лучшая документация кода  
❌ Немного больше кода

### Почему React Navigation?

✅ Стандарт де-факто для RN  
✅ Много готовых решений  
✅ Хорошая документация  
✅ Поддержка deep linking  

---

## 📊 Метрики проекта

- **Экраны:** 4
- **Сервисы:** 2
- **Компоненты:** ~6 (встроенные в экраны)
- **Функций:** ~15
- **Строк кода:** ~1500
- **Размер приложения:** ~50-70 MB (с зависимостями)
- **Время сборки:** 3-10 минут

---

## 🚀 Следующие шаги

1. **Установите зависимости:** `npm install`
2. **Запустите приложение:** `npm start`
3. **Протестируйте на реальном устройстве**
4. **Прочитайте FAQ.md** для частых вопросов
5. **Следуйте BUILD_GUIDE.md** для публикации

---

**Версия документации:** 1.0.0  
**Дата обновления:** 19 ноября 2025  
**Автор:** NFC Contact Share Team
